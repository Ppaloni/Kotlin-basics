/*
Create an array of integers with the following values [0, 3, 6, 9]. 
Use the Array class constructor. Print the first and the last elements of the array.
*/


//My solution

fun main(args: Array<String>) {

    val numbers = Array(4) {i -> i * 3}
    println("The first: " + numbers[0])
    println("The last: " + numbers[3])
}

